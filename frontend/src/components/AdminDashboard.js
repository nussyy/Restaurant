import React from 'react';
import './AdminDashboard.css'; // Import specific CSS file for AdminDashboard component

const AdminDashboard = () => {
  return (
    <div className="admin-dashboard">
      <h2>Admin Dashboard</h2>
      <p>Manage reservations, queries, and more from this dashboard.</p>
      {/* Add functionality for managing reservations, users, etc. */}
    </div>
  );
};

export default AdminDashboard;
